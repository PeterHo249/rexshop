<div class="edit_page">
            <div class="container">
                <div class="row">
                    <div class="col-md-12">
                        <h2 class="page-title">Edit order</h2>

                        <a href="/salesman/order" class="btn btn-primary">Back to all orders</a>
                        <br><br>

                        <form method="post" action="/salesman/order/edit/:id">
                            <table id="mytable" class="table table-bordred table-striped">
                                <tbody>
                                    <tr>
                                        <td>Staff ID</td>
                                        <td><input type="text" name="staff_id" value="{{order_item.staff_id}}" /></td>
                                    </tr>
                                    <tr>
                                        <td>Customer ID</td>
                                        <td><input type="text" name="customer_id" value="{{order_item.customer_id}}" /></td>
                                    </tr>
                                    <tr>
                                        <td>Cost</td>
                                        <td><input type="text" name="cost" value="{{order_item.cost}}" /></td>
                                    </tr>
                                    <tr>
                                        <td>Date</td>
                                        <td><input type="text" name="date" value="{{order_item.date}}" /></td>
                                    </tr>

                                    <tr>
                                        <td>Date</td>
                                        <td><input type="text" name="date" value="{{order_item.date}}" /></td>
                                    </tr>
                                    <tr>
                                        <td>Status</td>
                                        <td><input type="text" name="status" value="{{order_item.status}}" /></td>
                                    </tr>

                                    <tr>
                                        <td>Count</td>
                                        <td><input type="text" name="count" value="{{order_item.count}}" /></td>
                                    </tr>

                                    <tr>
                                        <td><button class="btn btn-primary" type="submit">Update</button></td>
                                    </tr>
                                </tbody>
                            </table>

                        </form>
                    </div>
                </div>
            </div>
        </div>